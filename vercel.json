{
  "buildCommand": "npm install && cd packages/dashboard && npx vite build",
  "outputDirectory": "packages/dashboard/dist",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
